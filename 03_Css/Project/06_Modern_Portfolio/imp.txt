/* 
main .cert_content .cert_card__container > .cert_card:hover {
    animation: roundBorder 3s infinite linear, jiggle var(--transition-speed) ease-out;
    transform: translateZ(0); /* Fixes 3D rendering issues */
    will-change: transform, box-shadow, border-color; /* Optimize rendering */
}

/* Continuous round border animation (fixed border size) */
@keyframes roundBorder {
    0% {
        border-color: var(--primary-color) transparent transparent transparent;
    }
    25% {
        border-color: transparent var(--primary-color) transparent transparent;
    }
    50% {
        border-color: transparent transparent var(--primary-color) transparent;
    }
    75% {
        border-color: transparent transparent transparent var(--primary-color);
    }
    100% {
        border-color: var(--primary-color) transparent transparent transparent;
    }
}

/* Jiggle effect */
@keyframes jiggle {
    0% {
        transform: rotate(0deg);
    }
    25% {
        transform: rotate(2deg);
    }
    50% {
        transform: rotate(-2deg);
    }
    75% {
        transform: rotate(2deg);
    }
    100% {
        transform: rotate(0deg);
    }
} */












/* main .cert_content .cert_card__container .cert_card .cert_view > a {
    width: fit-content;
    display: flex;
    gap: var(--gutter-mini);
    background-color: var(--text-color-dark);
    border-radius: var(--radius-large);
    padding: var(--p-small) var(--p-medium) var(--p-mini) var(--p-medium);
} */